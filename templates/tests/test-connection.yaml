apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-go-book-management.fullname" . }}-test-connection"
  labels:
    {{- include "helm-go-book-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-go-book-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
